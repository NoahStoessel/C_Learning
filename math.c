#include <stdio.h>
#include <math.h>

int main(){
    // square root
    double a = sqrt(9);
    printf("a");
    // value to the power of
    a = pow(17, 2);
    // round
    int b = round(5.6546);
    // ceil round number up
    b = ceil(3.4);
    // round down floor
    b = floor(3.4);
    // absolute value distance to zero makes any negative number positive and positive stays positive
    b = fabs(-100);
    // logarithms
    a = log(5);
    // sine
    a = sin(5);
    // cosine
    b = cos(45);
    // tangent
    b = tan(30);
    return 0;
}