#include <stdio.h>
#include <string.h>
#include <stdbool.h>
int main(){
    // for loops
    //repeat a certain action a set number of times
    //for(int i = 10; i <= 100; i++){ // first init index for loop then set end condition after decrement or increment the index
    //    printf("%d\n", i); // i++ can also be i+= or i /= or whatever
    //}
    //char name[50];
    //bool test = true;
    // while loop repeats an action infinetly or while a condition is met
  
    //while (true) {
    //}
    // do while loops
    // while only runs if condition is checked
    // do while first runs then checks
    do {
      printf("Hi new!\n");
    } 
    while(true);
    //char name[50];
    //fget(name, 50, stdin);
    //name[strlen(name) - 1] = '\0';
    //printf("Hello: %s", name)
    return 0;
}
